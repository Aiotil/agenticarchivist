# iPhone app

Swift copystand capture app. Not started.

It will reuse the existing capture pipeline (RAW exposure brackets, Metal frame averaging, Vision document detection) and use the Rust core through UniFFI. On the phone, a collection is a partial copy: thumbnails, previews, its own captures until two other devices hold them, and originals the person pins.

Bundle ID: `com.agenticarchivist.ios`.
